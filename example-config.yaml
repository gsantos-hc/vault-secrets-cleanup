vault:
  address: https://vault.example.com
  token: replace-with-token-or-use-env

rate_limit:
  requests_per_second: 100
  burst: 100

staleness:
  default_period: 365d
  namespaces:
    prod/*: 730d
    dev/*: 90d

exclusions:
  namespaces:
    - admin/*
    - system/*
  paths:
    - "*/bootstrap/*"
    - "*/root-token"
  mounts:
    - internal/*

logging:
  level: info
  format: text
